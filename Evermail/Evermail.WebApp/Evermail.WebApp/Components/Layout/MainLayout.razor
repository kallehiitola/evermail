@inherits LayoutComponentBase
@using System
@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Navigation

<div class="page @(ShouldHideSidebar ? "page--no-sidebar" : null)">
    @if (!ShouldHideSidebar)
    {
        <AuthorizeView>
            <Authorized>
                <div class="sidebar">
                    <NavMenu />
                </div>
            </Authorized>
        </AuthorizeView>
    }

    <main>
        <TopRow />

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<AiImpersonationBootstrapper />

@code {
    private bool ShouldHideSidebar =>
    Navigation.Uri?.Contains("/onboarding", StringComparison.OrdinalIgnoreCase) == true;
}